# 开源方案调研 —— "有没有现成的轮子可以直接抄？"

> 日期：2026-09-14 ｜ 目的：回答"这套表格图片转录工作流，到底能不能拿现成项目拼出来，避免重复造轮子"
> 方法：**逐个仓库实名核查**（GitHub 仓库搜索 API + PyPI 官方元数据 + 直接读项目 README 源码），不采信二手转述。

---

## 〇、先说检索条件本身（透明起见）

- 本机的 `web_search` 工具**不可用**（接口 401 鉴权失败），`web_fetch` 超时；
- 机器上有可用网络（本地代理 `127.0.0.1:7897`），我用它直连：
  - **GitHub 仓库搜索 API**（`api.github.com/search/repositories`）——拿 star 数与项目描述；
  - **PyPI 官方元数据**（`pypi.org/pypi/<包>/json`）——拿版本、发版日期、许可证；
  - **raw.githubusercontent.com**——直接读项目 README 原文；
- Bing 网页搜索经该代理返回的是**无关垃圾结果**（不可信），已弃用，所有结论均以 GitHub/PyPI 实名数据为准。

---

## 一、结论先行

**没有找到任何"拿来就能用"的现成项目。**

市面上的开源项目可以归成三类，每一类都**只覆盖了我们要么已经解决、要么不需要的那部分**：

| 类型 | 代表 | 为什么不能直接用 |
|------|------|------------------|
| **重型 IDP 平台** | Unstract (7,235★)、各类企业级套件 | 要 Docker Compose、要起服务、要配 Prompt Studio。你的场景是"工厂电脑双击就能用"，**运维成本比收益高** |
| **单点抽取工具** | 各种 table-extraction 小项目（多数 < 100★） | 只做"图 → 表"这一步，没有校验、没有审核闭环、没有归档，且大多无维护 |
| **研究型库** | google/langextract (38,578★) | **只处理文本，不处理图片**；但它的核心机制值得我们抄 |

**但这次检索不是白做的——有两个实打实的收获**，见第五节。

---

## 二、逐类核查结果（均为实名数据）

### 2.1 端到端文档智能平台

| 项目 | Stars | 结论 |
|------|-------|------|
| `Zipstack/unstract` | 7,235★ | LLM 抽取平台，有 Prompt Studio、API 部署、n8n 节点。**但要求 Docker & Docker Compose**。你的部署哲学是"解压 + 双击一个 bat"，引入 Docker 等于换一套运维体系 |
| `google/langextract` | **38,578★** | 见 2.4——最有价值的发现 |
| `docling-project/docling` | 66,362★ | IBM 出品，PDF/DOCX/HTML → 结构化中间表示。**PDF 优先**，你要的是"拍照/截图"，输入形态不匹配 |
| 各类 `document-understanding-pipeline` | 多在 100★ 以下 | 多为示例代码/研究复现，不是可交付产品 |

### 2.2 表格/OCR 单点工具

| 项目 | Stars | 版本(发版) | 结论 |
|------|-------|-----------|------|
| `PaddlePaddle/PaddleOCR` | 89,422★ | 3.7.0 (2026-06) | 中文最强，Apache-2.0。**但它做的是"图 → 文字/版面"，不是"图 → 业务字段"** |
| `opendatalab/MinerU` | 79,802★ | 3.4.5 (2026-08) | PDF 优先 |
| `datalab-to/marker` | 39,687★ | 2.0.0 (2026-07) | PDF → Markdown |
| `datalab-to/surya` | 21,383★ | 0.22.1 (2026-07) | 图片优先，含表格行列识别，Apache-2.0 |
| `RapidAI/RapidOCR` | 7,808★ | 1.4.4 (2025-01) | ONNX 跨平台，轻 |
| `microsoft/table-transformer` | 2,945★ | — | 表格结构识别专用模型 |
| 各类 `*-table-extraction-*` | 多在 10★ 以下 | — | 玩具级，无维护 |

**关键判断**：这些项目解决的问题是「**从图里把文字和表格结构抠出来**」。
而我们的流水线里，这一步**已经由多模态大模型完成了**（而且对中文、手写、拍照倾斜更鲁棒）。
它们**不解决**我们真正难的部分：校验、问题账本、集中审核、归档、可追溯。

### 2.3 人工审核 / 标注工具

搜到的是 `Label Studio` 类标注平台、`RootPainter` 类科研标注工具、若干 < 100★ 的
"human-in-the-loop annotation" 小项目。**没有一个是"表格识别结果集中审核"这个形态**——
它们面向的是"给数据打标签训练模型"，我们要的是"**让人一次看完所有疑点然后拍板**"。

### 2.4 ⭐ 最有价值的发现：google/langextract

- **38,578★**，`langextract` 1.7.0，Google 出品；
- README 原文要点：
  > **Precise Source Grounding**: Maps every extraction to its exact location in the source text, enabling visual highlighting
  > **Interactive Visualization**: generates a self-contained HTML file to visualize and review thousands of extractions
  > **Grounding:** LLMs may occasionally extract content from few-shot examples rather than the input text. LangExtract automatically [detects this]

**它解决的是我们的头号风险：模型编数据。**

它的答案是：**每一处抽取都必须能定位回原文的某个位置**，定位不上就自动识别为"没有依据"。
这和我们的 `raw_value`（图上原文）机制**是同一个思路**，而且我们已经有了——
我们甚至更进一步：用 `raw_value ↔ value` 自洽校验把它变成了**确定性判据**。

**它是文本库，不能直接用于图片**，但它验证了我们的方向是对的，
也提示了一个可以继续加强的点（见第六节）。

---

## 三、那 GPT 那份意见怎么评价？

GPT 的价格与许可证信息**基本准确**（我复核了 PaddleOCR = Apache-2.0、Surya = Apache-2.0，
与我查到的一致；Docling 的 MIT 说法来自其仓库页，我未逐条复核）。

但它有 **两处判断我不同意**：

### 不同意 1：「PaddleOCR 免费、直接装」

**不建议装。** 理由：

1. 它**撞你自己定的红线**——`架构说明.md` 第八节写着「**不装本地 OCR**」，
   理由正当：「每一条都会增加出错的可能性和维护负担」；
2. 它**和你的部署哲学冲突**——引入 paddlepaddle 是**数百 MB** 的依赖，
   "换电脑解压双击就好"这个最大的优势会直接赔进去；
3. 它**解决的是你已经解决的问题**——看图这一步多模态大模型已经做了，而且做得更好；
4. **误差是累积的**：在 VLM 之外再叠一层传统流水线，假警报会涌进问题清单，
   而人工审核是你最贵的一环。

**正确姿态**：不是"装了备用"，而是**"等它真的有用再说"**——
如果哪天出现**具体、可复现**的误读痛点（比如某类手写数字稳定读错），再针对性评估单点方案。

### 不同意 2：「pandas / matplotlib 直接用」

**现在不要加。** 它们是"以后做趋势分析"用的，不是当前流水线需要的。
每加一个依赖都要进 `vendor/wheels` 离线包、都要在换电脑时多一分风险。
等真有"想看 IPAC 里 DMF 三个月的趋势"这个需求时再加，那时候加也不迟。

### 同意的部分

- ✅ **钱应该花在模型调用上**，而不是买 IDP 平台——完全同意；
- ✅ **先做 20~30 张真实图的盲测**再决定要不要加东西——完全同意；
- ✅ **ABBYY / UiPath / LandingAI 这类企业级 IDP 不要考虑**——同意，和你的预算与形态都不匹配；
- ✅ **审核界面就该用 Excel，自己写反而合理**——同意，`task_review.xlsx` 就是"你的业务审核界面"，
  这不算造轮子。

---

## 四、我们真正需要自己写的部分（= 合理的自研边界）

这次调研反过来说明了一件事：**我们的核心价值恰好是开源生态没有覆盖的那块**。

| 自己写的部分 | 为什么没有现成轮子 |
|--------------|--------------------|
| **确定性裁判**（`validate_export.py`） | 开源库做的是"校验数据格式"，而我们要的是"**按你的业务规则判定对错**"（合计上限、同类词归类、身份不明的行不许落账） |
| **问题账本 + 集中审核** | 开源做的是"标注/打标签"，我们要的是"**一次看完所有疑点再拍板**" |
| **可追溯归档** | 没人做"识别完把原图按日期改名归档、绝不覆盖"这件事 |
| **版式契约**（`schemas/`） | 这是**你的业务知识**，买不到 |

**结论**：**不需要推倒重来，也不需要引入重型平台。** 这条路是对的。

---

## 五、这次调研带来的两个实际改进

### 改进 1：把技能通用化（已落地）

调研确认"没有现成端到端方案"后，把技能从"化工厂专用"改造成了**通用表格图片转录工作流**：

- 裁判脚本改为 **schema 驱动**：表格有哪些列、哪些必填、有没有合计、上限多少、
  同类词怎么归类、归档怎么命名——全部由 `schemas/*.json` 声明，**代码不认识任何行业**；
- 新增通用列类型校验（日期列写成乱码会被抓住）；
- 新增 `schemas/_default.json` 通用兜底契约 + 两份化工作为**示例**；
- `schemas/README.md` 写成了"怎么给新表型加契约"的作业指导书；
- 没注册表型也能跑（走通用兜底），不会崩。

**现在换成报销单、考勤表、报价单，走的是同一套流程。**

### 改进 2：把验证固化成回归测试（已落地）

新增 `tools/回归测试.py`：**29 个"已知答案"的用例**，一条命令全跑完。

- 在系统临时目录搭隔离工作区，**只读真实数据、跑完自动清理**；
- **样本内置在测试里**，不依赖工作区数据——所以在**全新的电脑上照样能跑**（已实测）；
- 覆盖：历史行为一致性、人工决定不污染数据、未解决行不落账、
  畸形输入不崩、路径逃逸被拒、归档安全、通用表型、自定义合计规则…

这是"数据不出错"这件事的**唯一可持续保障**。

---

## 六、值得继续观察的方向（不是现在做）

| 方向 | 说明 | 触发条件 |
|------|------|----------|
| **借 langextract 的"必须可定位"思路** | 我们现在只对**计量项**要 `raw_value`。可以扩展成：**每个字段都要求给出图上原文片段**，脚本检查"这个值是否真的在这张图读到的文字里"。这是把"防编造"从成分扩展到全字段 | 当出现"字段级编造"的真实案例时 |
| **离线 OCR 做交叉验证** | 对**具体某类稳定读错的图**，用 PaddleOCR 做第二证据源 | 出现具体、可复现的误读痛点时 |
| **pandas / DuckDB 做趋势** | 数据攒到几千行、要看趋势时 | 有明确的统计需求时 |
| **图片质量预检** | 拍照过糊/过暗时提前提示重拍，省掉一次模型调用 | 实际出现大量废片时 |

**共同原则**：**每加一样东西，都要先说清它解决了哪个已经发生的具体问题。**
不要因为"它先进""它免费"就加——免费的东西也有安装、维护和出错成本。

---

## 附：本次核查过的项目清单（实名）

```
google/langextract            38,578★   langextract 1.7.0
PaddlePaddle/PaddleOCR        89,422★   paddleocr 3.7.0   Apache-2.0
opendatalab/MinerU            79,802★   mineru 3.4.5
docling-project/docling       66,362★   docling 2.126.0
datalab-to/marker             39,687★   marker-pdf 2.0.0  Apache-2.0
datalab-to/surya              21,383★   surya-ocr 0.22.1  Apache-2.0
allenai/olmocr                19,468★
QwenLM/Qwen3-VL               19,936★
Unstructured-IO/unstructured  15,421★   unstructured 0.27.5
opendatalab/PDF-Extract-Kit   10,012★
rednote-hilab/dots.ocr         9,108★
Ucas-HaoranWei/GOT-OCR2.0      8,219★
RapidAI/RapidOCR               7,808★   rapidocr-onnxruntime 1.4.4  Apache-2.0
Zipstack/unstract              7,235★
microsoft/table-transformer    2,945★
pandera-dev/pandera            4,452★   pandera 0.33.1
pydantic/pydantic             28,762★   pydantic 2.13.5
jmcnamara/XlsxWriter           3,972★   XlsxWriter 3.2.9
anthropics/skills            176,049★   （Agent Skills 官方仓库）
```
