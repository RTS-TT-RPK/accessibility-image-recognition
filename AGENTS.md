# 表格图片转录工作区 —— 行为手册（AGENTS.md，V2.0）

> 任何 AI / Codex 在本目录工作时：先读本文件，再动手。
> 四不原则：**不猜、不造数、不覆盖、不静默。**

## 1. 这是什么

把**表格图片**（手机拍的纸质单据、电脑截图、扫描件；印刷体或手写体；任意行业）
转录成结构化 Excel 的工作区：

```
看图(多模态大模型) → 出原文 raw.json → 脚本确定性校验(唯一裁判)
  → 问题集中成账本 → 人一次性审核 → 重跑落账 → 归档原图
```

**这套流程不绑定行业。** 具体表格长什么样（有哪些列、哪些必填、有没有合计、
同类词怎么归类、归档怎么命名），全部由 `schemas/` 里的**版式契约**描述；
裁判脚本本身是通用的。化工厂的来料分析单只是已注册的表型之一。

## 2. 目录分工

| 路径 | 作用 |
|------|------|
| `inbox/` | 用户扔图的地方。不预分类、不改文件名、不删除 |
| `pending/` | 读不清/待确认的原图暂存 |
| `archive/YYYY-MM-DD/` | 归档区（人工确认后才移动进来） |
| `runs/YYYYMMDD_NNN/` | 每次任务的存档（raw/issues/decisions/archive_plan/final/task_review/source） |
| `schemas/` | **版式契约**（每种表格一个 JSON）。加新表型看 `schemas/README.md` |
| `aliases.json` | 同类词词典（只存用户拍板过的映射，对所有表型生效） |
| `FEEDBACK_LOG.md` | 错题本 |
| `golden/` | 人工确认过的标准答案（只追加，永不修改） |
| `validate_export.py` | 唯一裁判脚本（通用、schema 驱动）。改动须人拍板，且必须跑回归测试 |
| `tools/回归测试.py` | 一条命令验证裁判行为没被改坏（只读真实数据、跑完自动清理） |
| `docs/` | 只放历史存档（旧方案/旧评审）。当前架构与安装文档在 `技能源文件/image-data-recognition/references/`，见 `docs/README.md` |
| `技能源文件/image-data-recognition/` | 技能唯一真源（SKILL.md + references + agents）；改动后重跑 `tools/安装技能（双击）.bat` 同步到各工具 |
| `tools/` | 打包、安装、自检、回归测试等辅助工具 |
| `vendor/` | 离线依赖（换电脑用） |


> **骨架自检**：换电脑、或刚解压出来的工作区，第一次干活前先跑
> `python tools/初始化工作区.py` —— 补齐 `inbox`、`pending`、`archive`、`golden`、`runs` 等文件夹。
> 它只创建目录和说明文件，**不删、不覆盖、不动任何已有数据**，重复运行无副作用。
> 缺 `inbox` 用户没地方放图；缺 `runs` 裁判脚本会直接报"目录不存在"退出。

## 3. 一次任务的固定流程（三阶段）

> **模型无关**：任何有多模态能力的模型都能干这活。模型与流程之间只有一个接口
> ——`raw.json`。换模型不影响历史数据、不影响已填的人工决定、不需要改脚本。
> 判断模型够不够用：**看它做出来的样板里，该标 `uncertain` 的地方是不是老实标了**。

### 阶段一：立样板（1 张图，人确认一次）

1. **收图建任务**：新建 `runs/YYYYMMDD_NNN/`，原图复制进 `source/`，逐张算 sha256。
2. **挑样板图**：挑**有代表性**的（不要挑最清楚的），明显两种版式就做两个样板。
3. **只识别这一张**，写进 `raw.json`（`images` 先只放它）。
4. **生成样板**：`python tools/生成样板.py runs/YYYYMMDD_NNN`
   → 产出 `样板.xlsx`（给人看，第 5 页签确认）+ `样板_契约草案.json`（给 AI 用）。
5. **交用户确认**：请他看 `样板.xlsx`，在第 5 页写意见。来回几次都正常，数据不会丢。
6. **确认后**：把草案整理成 `schemas/<表型名>_v1.json`；写 `sample.json` 记录已确认；
   跑 `python tools/回归测试.py` 确认没改坏。
7. 已有契约的表型**可跳过本阶段**。

### 阶段二：批量转录（闷头干完，不许中途打扰用户）

8. 照冻结的契约**逐张识别剩余图片**；看不清就记 `uncertain`，**不要停下来问**；
   版式跟样板不一样就照实记、标好 `row_role`，**不要自己发明新列**。
9. **每识别完一张立刻并入 `raw.json` 落盘**，绝不攒到最后写。
10. 全部识别完后刷新账本：`python validate_export.py runs/YYYYMMDD_NNN --init-issues`

### 阶段三：一次性复核

11. **生成审核表**：`python validate_export.py runs/YYYYMMDD_NNN`
12. **交审核**：把 `task_review.xlsx` 交给用户，请其**只填"问题清单"的员工决定列**。
13. **收决定**：用户回复"审核完成"后，把 Excel 内容整理成 `decisions.json`（见第 6 节）。
    **这步不能省**——Excel 是派生的、重跑会重写，只有 `decisions.json` 是权威。
14. **重跑+归档**：`python validate_export.py runs/YYYYMMDD_NNN --archive`
15. **汇报**：一句话交付（表型、行数、问题数、归档结果、文件路径）。

## 4. 红线（永不违反）

- **不猜**：看不清就进 `uncertain`；绝不替用户决定业务口径。
- **不造数**：机器永不生成"算出来"的数据（例如自动补"杂分"）。占位词有值就照录、空白就留白。
- **不覆盖**：归档一律交给脚本（内部用 os.rename，撞名自动加序号）；绝不删除 `inbox/`、`pending/` 里未处理的图；绝不修改 `golden/` 的历史条目。
- **不改裁判**：`validate_export.py` 是唯一裁判，改动必须由人拍板；
  **改完必须跑 `python tools/回归测试.py` 且全绿**，否则不许提交。
- **不静默**：任何"我改了/我跳过了/我没看懂"都必须在汇报里说清。
- **合计规则由 schema 定**：每张表的合计上限写在 `schemas/<表型>.json` 的
  `measurements.sum_max`（默认 100%）。**超过上限一律进问题清单，无容差**；
  **低于上限不补、不提示**。"水"这类永不计入合计的类别也由 schema 声明。

## 5. raw.json 结构（识别输出）

```json
{
  "run_id": "20260910_001",
  "created": "2026-09-10 20:30:00",
  "source_type": "inventory | incoming",
  "images": [
    {"image_id": "IMG_0001", "file": "runs/20260910_001/source/IMG_0001.jpg",
     "sha256": "...", "sheet_label": "来料分析", "date_text": "2026-09-10",
     "suggested_name": "2026-09-10_来料分析_批A001_第1张.jpg"}
  ],
  "rows": [
    {"row_id": "R001", "image_id": "IMG_0001", "sheet": "来料分析",
     "row_role": "data | summary | header | note",
     "object_label": "A001/桶01", "required_fields": ["物料"],
     "fields": {"日期": "2026-09-10", "位置": "北罐区7#", "物料": "IPAC", "批次": "A001", "桶号": "01"},
     "components": [
        {"name": "IPAC", "raw_value": "89.6%", "value": "0.896", "unit": "%"}
     ],
     "uncertain": [{"field": "乙酸", "candidates": ["0.5%", "5%"], "note": "字迹不清"}]}
  ]
}
```

要点：
- 一行 = 一个对象（一桶 / 一个样品 / 一个储位 / 一个员工一天）；
- `fields` 放这张表的普通列（列名随意，由 schema 声明）；`components` 放**计量项**
  （成分/指标之类需要合计校验的项）。**普通表格可以完全没有 components**——
  例如报价单、考勤表，那就不会有合计校验，走同一套流程；
- `raw_value` 是图上原文、`value` 是换算值，**两者都要有**（脚本会查自洽）；
- 表头、合计、备注行必须标 `row_role`；**身份不明的行会被拦下**，宁可不落账也不错落账；
- 看不清进 `uncertain`，**绝不猜**。

## 6. decisions.json 结构（人工决定的落点）

```json
{
  "run_id": "20260910_001",
  "issue_decisions": [
    {"issue_id": "ISS-SUM-R002-", "decision": "DMF=0", "note": "对照原图"},
    {"issue_id": "ISS-OCR-R003-乙酸", "decision": "0.5%", "note": ""},
    {"issue_id": "ISS-MISS-R005-物料", "decision": "IPAC", "note": ""}
  ],
  "archive_decisions": [
    {"image_id": "IMG_0001", "confirmed": true, "suggested_name": "（可选，用户改的名字）"},
    {"image_id": "IMG_0002", "confirmed": false}
  ]
}
```

决定写法：
- `字段名=值` → 修正对应字段 / 计量项（图上读不清的也可用，会补进去）；
- `通过 / 确认无误 / 照录` → 按原样接受，不再阻塞；
- `沿用候选A / 沿用候选B` → 采用问题清单给的候选值（脚本自动还原，不会把标签当数据）；
- `见备注` → 只看备注、不改数据（该行保持阻塞，直到给出真实值）。

## 7. 新表型的注册办法（走样板，不要用问卷）

**不要用一串抽象问题去问车间员工**（"这张表一行代表什么？有哪些列？"）——太抽象。
正确做法是**做出来给他看**，即第 3 节的阶段一：

1. 挑一张有代表性的图 → 只识别它 → `python tools/生成样板.py runs/YYYYMMDD_NNN`；
2. 把 `样板.xlsx` 交用户，他在第 5 页签确认意见；
3. 用户确认后，把 `样板_契约草案.json` 整理成 `schemas/<表型名>_v1.json`
   （完整字段说明见 `schemas/README.md`）；
4. 写 `sample.json` 记录确认；跑一次 `python tools/回归测试.py` 确认没改坏；
5. 同一表改版 = 新增 `_v2`，旧版永久保留（历史照片按旧版解析）。

**没注册表型也不会崩**：脚本会用 `schemas/_default.json` 的通用口径跑，
只是没有专属的列/必填/合计规则，并且会提示"表名不在已注册契约里"。

## 8. 任务中断与断点续跑（员工离开也没关系）

**原则：每处理完一张图就立刻落盘，绝不攒到最后一次性写。** 这样任何时候断电、关机、人走开，活都停在"已完成的部分"，重新开工不会从零开始。

### 恢复工作前，先看进度

```
python validate_export.py runs/YYYYMMDD_NNN --status
```

输出会明确告诉你：
- **当前处在哪个阶段**（① 未开始 / ② 立样板 / ③ 批量转录中 / ④ 待统一复核）；
- 今天是几张图、已经识别了几张、**还剩哪几张没识别**（按文件名列出）；
- 校验做了没有、有多少问题、还差几条没人工处理；
- 最终数据和归档做完了没有；
- 最后一行直接给"下一步该干什么"。

### 中断后的固定动作

1. 跑 `--status`，看清楚停在哪一步；
2. **只补没做完的部分**（例如只剩 2 张图没识别，就只识别这 2 张，不要重跑全部）；
3. 继续按第 3 节的流程往下走。

### 硬规则

- **识别阶段每完成一张图，就把结果并入 `raw.json` 落盘**（不要等全部图片都看完再写）。
- 用户问"进度怎么样 / 做到哪了"，直接跑 `--status` 把输出讲给他听，不要凭记忆回答。
- 重复跑同一条命令是安全的：脚本会跳过已完成的，保留已填的人工决定。
- 任务被中断（AI 停止、断电、关机）**不会丢任何东西**——所有中间结果都在 `runs/` 里。

### 让任务自己跑完（可选）

如果希望"人走开、活照跑"，可在 Codex 里挂一个**心跳任务**：定期检查当前任务是否停在半路，停了就自动接着干，干完保持安静。设置方式见 `使用说明.md`。

## 9. 出错怎么办

| 情况 | 处理 |
|------|------|
| 图看不清/倾斜/太暗 | 请用户重拍；原图放 `pending/` |
| 校验报错（合计超上限等） | 进问题清单，请用户对照原图给正确值；不要自行修改 |
| 用户填的决定里数值不合格 | 指出并请其确认，不要替其换算 |
| 归档目标同名 | 脚本自动加 `_2`；绝不覆盖 |
| 任务跑一半中断 | 重跑同一条命令；已落盘内容会复用，人工决定不会丢 |
| 脚本报"结构异常" | `raw.json` 有畸形（如 `fields: null`）。脚本会尽量救回并如实报出，**在汇报里说明** |
| 改了裁判脚本 | 必须跑 `python tools/回归测试.py`，全绿才算改对 |

## 10. 读文件与编码（Windows 换电脑必踩的坑）

本工作区所有文本文件都是 **UTF-8**。但 Windows PowerShell 5.1（Desktop 版）的
`Get-Content` 不带 `-Encoding` 时按**系统 ANSI 代码页**解码——中文 Windows 上是 **GBK/936**，
于是没有 BOM 的 UTF-8 中文会被读成乱码：

```
图像识别长期工程   →   鍥惧儚璇嗗埆闀挎湡宸ョ▼
```

**硬规则**：

1. 读工作区里任何文本文件，**优先用 read 工具**（按 UTF-8 处理，且能正确跳过 BOM）；
2. 必须用 shell 读时，PowerShell 5.1 下**一律显式写编码**：`Get-Content -Encoding UTF8 <文件>`；
3. 读到以 `\ufeff` 开头的路径/内容，**先去掉 BOM 再用**，不要把 BOM 当成路径的一部分；
4. 写技能副本里的 `workspace.txt`（一行工作区绝对路径）**必须用 UTF-8 带 BOM**：
   - Python 用 `encoding="utf-8-sig"` 写；
   - Python 读也要用 `encoding="utf-8-sig"`（用 `"utf-8"` 会多出 `\ufeff`，路径就废了）；
   - 手动用记事本另存为时选 **"UTF-8 带 BOM"**；
5. `SKILL.md` **绝不能加 BOM**——YAML frontmatter 必须以 `---` 开头，加了 BOM 技能就解析不了；
6. 遇到乱码**不要猜原路径**，停下来换正确编码重读一遍，或直接问用户。

> 换电脑/新解压工作区时的完整安装与环境检查步骤，见根目录 `AI自安装说明.md`；
> 一句命令看环境是否就绪：`python tools/环境自检.py`。
