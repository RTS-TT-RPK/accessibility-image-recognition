# 使用说明（大白话版）

## 一、这个系统是干什么的

你把**表格图片**丢进来——手机拍的单据、电脑截图、扫描件，印刷的、手写的都行——
系统帮你：
1. 自动看懂图上的表，读成数据；
2. 拿不准的地方**集中列出来问你一次**（不是一条条烦你）；
3. 你确认后，生成干净的 Excel；
4. 把原图按日期归档好、改好名字，方便以后查。

**它不会做的事**：不会瞎猜数字，不会替你补一个"算出来的"数，不会偷偷改你确认过的数据。

> **不限于某一种表格。** 换一种没见过的表（报销单、考勤表、报价单…），
> 系统会先问你几个问题（这张表一行是什么、有哪些列、有没有合计），
> 确认后记成一份"版式契约"，以后同类表格就照它自动校验。
> 契约放在 `schemas` 文件夹里，可以自己看、也可以让 AI 改。

---

## 二、怎么用（先看一次样板，最后审一次）

整个流程里**你只需要出现两次**：确认样板、复核问题。

### 第一次：确认样板（只花几分钟）

1. **扔图**：把照片放进 `inbox` 文件夹。不用分类、不用改名、不用管顺序。
2. **说一句**：对 AI 说 **"处理今天的图"**。
3. **看样板**：如果是**没录过的新表**，AI 会先拿**一张**图做出来给你看——
   打开 `样板.xlsx`：
   - 第 1 页：**我打算这样录**（表名、一行是什么、有哪些列、有没有合计）；
   - 第 2 页：**这张图读出来的内容**（照录出来的实际数据）；
   - 第 4 页：**看不清的地方**；
   - 第 5 页：**请在这里确认**——你在右边填几个字就行（"对" / "桶号改成罐号" / "含水量就是水分"…）。
   - 填完保存、关掉，对 AI 说 **"样板没问题"**（或把你改的地方说清楚）。

> **为什么先做一张给你看？** 因为直接问你"这表有哪些列、一行代表什么"太抽象；
> 做出来给你看一眼，你立刻就知道对不对。

### 中间：AI 自己干完，不会打扰你

- 样板确认后，AI 会照这个模式**把剩下的图一口气识别完**；
- 遇到看不清的、拿不准的，**它只记账、不会一条条来问你**；
- 每识别完一张就立刻存一次，所以**中途断电、下班走人都不丢进度**。

### 第二次：审一次（最后统一看）

4. **审一次**：全部图处理完，AI 会把 `task_review.xlsx` 给你。
   - 你只需要看两个地方：**审核总览**（今天录了几张、几个表、多少行、几个问题）
     和 **问题清单**（有疑问的地方，一行一条，填"员工决定"这一列）。
   - **没有问题的数据不用管**，默认就是对的。
   - 填完保存、关掉 Excel，对 AI 说"**审核完成**"。
   - 它会重新算一遍，生成最终 Excel（`final.xlsx`），并把照片归档。

> **录过的表不用再做样板**：第二次遇到同一种表格，AI 直接照上次确认的模式批量处理。

---

## 三、一个技能，多种 AI 工具都能用

这套东西**不绑定某一个 AI 工具**。Codex、DSH、ZCode、Cursor、Claude、OpenCode ——
只要它认识 "SKILL.md" 这种技能文件，就能用。

### 怎么装到别的工具里

**双击 `tools\安装技能（双击）.bat`**，它会自动：
1. 检测你这台电脑装了哪些 AI 工具；
2. 把技能装进每一个工具的目录；
3. 顺便在工作区里也放一份（有些工具打开文件夹就直接认）。

只想看检测结果、先不安装：命令提示符运行 `python tools\安装技能.py --list`

### 什么时候要重跑这个脚本

- 技能内容更新了（AI 帮你改了文档）；
- 换了新电脑；
- 新装了一个 AI 工具；
- 工作区换了位置（改盘符或文件夹名）。

### 技能装在哪（想手动弄的话）

| 工具 | 复制到这里 |
|------|-----------|
| Codex | `C:\Users\你的用户名\.codex\skills\` |
| DSH | `C:\Users\你的用户名\.dsh\skills\` |
| Cursor | `C:\Users\你的用户名\.cursor\skills\` |
| Claude | `C:\Users\你的用户名\.claude\skills\` |
| OpenCode | `C:\Users\你的用户名\.opencode\skills\` |
| ZCode | `C:\Users\你的用户名\.zcode\skills\` |
| 通用标准 | `C:\Users\你的用户名\.agents\skills\` |

要复制的是 **`技能源文件\image-data-recognition\`** 这个文件夹（整个搬过去）。

### 换个工具、换个电脑，AI 怎么找到工作区？

技能里不写死路径。每个技能副本里有一个 `workspace.txt`，写着一行工作区位置。
AI 打开技能后第一件事就是读它，所以：**工作区搬到哪，重跑一次安装脚本就行。**

---

## 四、以后换电脑怎么搬（重点，三分钟搞定）

**核心：整个文件夹就是完整系统，复制过去就能用。**

### 搬家的步骤（新电脑上只要两步）

1. **打包**：在旧电脑上双击 `tools\打包技能包.bat`。
   - 会生成一个压缩包，在 `发布` 文件夹里，名字形如 **`表格识别技能包_V1.6_日期.zip`**。
2. **拷贝**：把这个压缩包用 **U盘 / 微信 / 钉钉 / 网盘** 传到新电脑（不需要 GitHub，不需要任何账号）。

到了新电脑上：

3. **解压**：右键压缩包 → 解压，得到 `表格识别系统` 文件夹。
   - **这个文件夹就是完整工作区**：`inbox`（扔图）、`runs`（任务存档）、`archive`（归档）、
     `golden`（标准答案）、`pending`、`schemas` 全都在里面，**你不用自己建也不用复制任何文件夹**。
4. **一键安装**：双击里面的 **`tools\一键安装（双击）.bat`**。它会一口气做完三件事：
   - 检查有没有 Python（没有会中文提示你怎么装）；
   - 自动装好所需的库（**离线安装，不需要联网**）；
   - 把技能装进新电脑上的每一个 AI 工具（Codex / DSH / Cursor / Claude / OpenCode / ZCode）。
5. **开工**：用任意一个 AI 工具打开 `表格识别系统` 这个文件夹，把照片丢进 `inbox`，说"处理今天的图"即可。

> 解压到哪个盘、哪个目录都行，文件夹名字也可以改——安装脚本会把当前路径记下来，技能自己就能找到工作区。

> 想分步来（比如只想装技能、不想动环境）：单独双击 `tools\安装环境（双击）.bat` 或 `tools\安装技能（双击）.bat` 也可以。
> 工作文件夹万一没补上，双击 `tools\初始化工作区.py`，或直接对 AI 说"帮我补齐工作区文件夹"。

### 让 AI 自己装（不用你动手）

如果你懒得上手，直接把这几句话发给新电脑上的 AI：

> 打开 `表格识别系统` 文件夹，读根目录的 **`AI自安装说明.md`**，照着把环境、工作文件夹和技能都装好，装完告诉我结果。

那份文档是专门写给 AI 看的：它会自己检查有没有 Python、缺依赖就装（优先用自带的离线包），
把技能装进它自己的技能目录，最后跑一遍自检并给你一份结果汇报。**手动步骤在文档第 7 节，也都在。**

### 为什么不用 GitHub

GitHub 是给程序员用的。你们这种场景**根本不需要**：文件复制 + 双击安装脚本，两步就够了。
（将来如果想让多台电脑同步，也可以把这个文件夹放公司共享盘，但不建议用微信直接传运行中的文件。）

### 想连同历史数据一起搬

`打包技能包.bat` 默认只打包**系统本身**（不含业务数据）。
如果你要把 `golden`（标准答案）和 `runs`（历史任务）也一起搬，在命令行里运行：

```
python tools\导出技能包.py --with-golden
```

---

## 五、常见问题

**Q1：双击 bat 一闪而过？**
说明报错太快没看清。右键 → 以管理员身份运行，或先打开"命令提示符"再运行。

**Q2：提示没有 Python？**
去 https://www.python.org/downloads/windows/ 下载 3.10 以上版本，安装时**一定要勾选** "Add python.exe to PATH"，装完再双击一次安装脚本。

**Q3：Excel 打不开生成的文件？**
确认用的是 Office Excel 或 WPS 都行；如果提示文件被占用，先关掉之前打开的 Excel 再点"审核完成"。

**Q4：我填错了想改？**
直接改 `decisions.json`（在 `runs\日期_编号\` 里），或对 AI 说"第 X 条改成 XX"，然后重跑即可（已填内容不会被冲掉）。

**Q5：归档的照片名字不喜欢？**
改 `runs\日期_编号\decisions.json` 里 `archive_decisions` 的 `suggested_name` 字段再重跑；或直接在对话里说"把 XX 照片改成 YY 名字"。

**Q6：某个 AI 工具里看不到这个技能？**
1. 先重跑一次 `tools\安装技能（双击）.bat`；
2. 重启那个工具；
3. 还不行就对它说："技能没被识别到，帮我查一下你读哪个目录。"

---

## 六、人走开了，活会停吗？

**不会。** 系统每处理完一张照片就立刻存一次，所以：

- 你中午去吃饭、下班回家、甚至电脑断电——**已完成的活不会丢**；
- 回来后对 AI 说一句 **"进度怎么样"**，它会告诉你：今天几张图、识别了几张、**还剩哪几张**、有没有要你确认的问题、下一步做什么；
- 想从断的地方接着干，就说 **"继续处理"**，它会只补没做完的部分，**不会从头再来一遍**（你之前填过的决定也不会被覆盖）。

> 想要"人不在也自动往下跑"：让管理员在 AI 工具里开一个**定时检查**（心跳任务），设定为每隔几分钟看一眼任务有没有停住，停住就自动接着做。这样就真正做到了"你忙你的，活自己跑"。

---

## 七、文件夹里都是什么

| 文件夹/文件 | 是什么 |
|------|--------|
| `inbox` | 你扔图的地方 |
| `archive` | 归档好的原图（按日期分文件夹） |
| `runs` | 每次任务的完整存档（含问题账本、审核记录） |
| `golden` | 你确认过的标准答案（越攒越多，系统越来越准） |
| `schemas` | 每种单据的识别契约 |
| `技能源文件` | 技能的唯一真源（改了要重跑安装脚本）；**当前的架构说明、安装部署说明都在这里** |
| `docs` | 只放历史存档（旧方案、旧评审），当前文档不在这里，见 `docs\README.md` |
| `tools` | 安装 / 打包 / 自检工具 |
| `validate_export.py` | 核心脚本（不要手工改） |
