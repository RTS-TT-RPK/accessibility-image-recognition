# 安装部署说明 —— 图像数据识别转化

> 目标：换电脑、换工具，都不用懂技术。**技能就是一个文件夹，复制到对应位置即可。**

---

## 一、一句话说清这套东西的结构

| 部分 | 是什么 | 在哪 |
|------|--------|------|
| **技能源文件** | 告诉 AI"这件事怎么做"的说明书（唯一真源） | `图像识别长期工程\技能源文件\image-data-recognition\` |
| **工作区** | 你的数据、脚本、规则、归档 | `图像识别长期工程\`（本文件夹） |
| **已安装副本** | 各家 AI 工具实际读取的那一份 | 见下面第四节表格 |

**关键理解**：
- 技能 = 说明书，每个工具要装一份（工具只认自己家的目录）；
- 工作区 = 数据工厂，只有一个；
- **技能源文件跟着工作区走**，改完重新跑一次安装脚本，所有工具同步更新。

---

## 二、本机现状（已经装好）

工作区：`C:\Users\86173\Desktop\图像识别长期工程\`
技能名：**图像数据识别转化**（英文标识 `image-data-recognition`）
依赖：本机已有 Python 3.12 + openpyxl（无需再装）
版本库：工作区里已建好本地 Git，随时可回退，不需要联网、不需要账号

---

## 三、装上/更新技能（一步）

**双击 `tools\安装技能（双击）.bat`** 即可。

它会自动：
1. 检测本机装了哪些 AI 工具（看配置目录在不在）；
2. 把技能复制到每个工具的技能目录；
3. 往每份副本写一个 `workspace.txt`，写明工作区在哪（这样 AI 自己就能找到工作区）；
4. 顺便在工作区里也放一份（`项目级`），有些工具打开文件夹时会直接读到。

只想看看检测结果、先不安装：命令提示符里运行 `python tools\安装技能.py --list`

**什么时候要重跑这个脚本？**
- 技能源文件改了（比如 AI 帮你更新了文档）；
- 换了一台电脑；
- 新装了一个 AI 工具；
- 工作区挪了位置（换了盘符或文件夹名）。

---

## 四、技能装到哪了（对照表）

### 各工具的"技能家目录"

| 工具 | 技能目录（`%USERPROFILE%` 就是 `C:\Users\你的用户名`） | 状态 |
|------|------------------------------------------------------|------|
| Codex | `%USERPROFILE%\.codex\skills\image-data-recognition\` | 已实测 |
| DSH | `%USERPROFILE%\.dsh\skills\image-data-recognition\` | 已实测 |
| Cursor | `%USERPROFILE%\.cursor\skills\image-data-recognition\` | 按官方文档 |
| OpenCode | `%USERPROFILE%\.opencode\skills\image-data-recognition\` | 已实测 |
| Claude | `%USERPROFILE%\.claude\skills\image-data-recognition\` | 通用约定 |
| ZCode | `%USERPROFILE%\.zcode\skills\image-data-recognition\` | 同族推测，装入后在工具里确认一次 |
| 通用 agents 标准 | `%USERPROFILE%\.agents\skills\image-data-recognition\` | 跨工具新标准 |

### 工作区里自带的（跟着文件夹走）

| 位置 | 谁会读 |
|------|--------|
| `图像识别长期工程\.dsh\skills\image-data-recognition\` | DSH（优先级最高） |
| `图像识别长期工程\.agents\skills\image-data-recognition\` | DSH 及遵循 agents 标准的工具 |

**好处**：把工作区文件夹整个拷到别的电脑，用支持项目级技能的工具（比如 DSH）打开它，技能自动就在，不用重新安装。

---

## 五、技能文件夹里都有什么

```
image-data-recognition\
├── SKILL.md                    ← 技能主文件（AI 照它执行）
├── workspace.txt               ← 安装时自动生成：写着一行工作区绝对路径
├── agents\openai.yaml          ← Codex 界面显示名（其他工具会忽略）
└── references\
    ├── 架构说明.md              ← 系统怎么设计的、为什么这么设计
    └── 安装部署说明.md          ← 本文件
```

`workspace.txt` 是整套东西能"到处跑"的关键：技能里不写死路径，AI 每次先读它。

---

## 六、换电脑搬家（三步）

### 第 1 步：在旧电脑上打包

双击工作区里的 **`tools\打包技能包.bat`**
→ 在 `发布` 文件夹里生成一个压缩包（约 0.3 MB，比一张照片还小）。

> 想把"标准答案库 golden"也一起带走（推荐长期做）：
> 命令提示符运行 `python tools\导出技能包.py --with-golden`

### 第 2 步：把压缩包拷到新电脑

U 盘 / 微信 / 钉钉 / 网盘 / 公司共享盘，任选。
**不需要 GitHub、不需要注册账号、不需要联网。**

### 第 3 步：在新电脑上安装

1. 解压压缩包，得到"表格识别系统"文件夹；
2. 双击 **`tools\安装环境（双击）.bat`** —— 检查 Python、离线安装依赖库；
3. 双击 **`tools\安装技能（双击）.bat`** —— 把技能装进新电脑上所有 AI 工具；
4. 用 AI 工具打开这个文件夹即可开始。

> 新电脑上装技能这一步，也可以**直接让 AI 自己做**：把工作区交给它，说一句
> "把这个技能装到本机所有 AI 工具里"，它会读 `技能源文件` 并完成复制。

---

## 七、如果没有 Python / 不想用脚本

手动复制也行，效果一样：

1. 找到 `图像识别长期工程\技能源文件\image-data-recognition\` 这个文件夹；
2. 整个复制过去，放到下面任一目录里（没有就自己建）：

```
Codex      →  %USERPROFILE%\.codex\skills\
DSH        →  %USERPROFILE%\.dsh\skills\
Cursor     →  %USERPROFILE%\.cursor\skills\
Claude     →  %USERPROFILE%\.claude\skills\
OpenCode   →  %USERPROFILE%\.opencode\skills\
ZCode      →  %USERPROFILE%\.zcode\skills\
```

3. 复制完，在技能文件夹里新建一个 `workspace.txt`，里面写一行工作区的完整路径，例如：
   `C:\Users\86173\Desktop\图像识别长期工程`
   （不写也行，AI 会自动找；写了它就不用找。）

---

## 八、技能自检与自修复（AI 做的事）

技能被加载后，AI 第一件事是**确认工作区在哪**，顺序是：

1. 读技能目录下的 `workspace.txt`；
2. 路径有效 → 直接用；
3. 无效或缺失 → 在当前打开的项目里找 `validate_export.py`；
4. 还找不到 → 在桌面找名字含「图像识别长期工程」的文件夹；
5. 全找不到 → **直接问用户**，拿到答复后写回 `workspace.txt`。

这条链保证了：工作区挪了位置、换了电脑、换了盘符，技能都不会"装死"。

自检命令（可选，想确认技能装没装好时用）：

```powershell
# DSH 专项自检：会打印 DSH 到底扫描哪些目录、有没有发现本技能
node tools\DSH技能自检.mjs "C:\Users\86173\Desktop\图像识别长期工程"
```

---

## 九、文件都在哪（全景图）

### 工作区 `图像识别长期工程\`

| 路径 | 是什么 | 要不要备份 |
|------|--------|------------|
| `技能源文件\` | 技能的唯一真源（改这里，然后重跑安装脚本） | **要**（很小） |
| `inbox\` | 你扔照片的地方 | 不用（处理完会归档） |
| `archive\` | 归档好的原图（按日期分文件夹） | **要**（原始凭证） |
| `runs\` | 每次任务的完整记录（识别原文、问题账本、你的决定、最终 Excel） | **要**（追溯全靠它） |
| `golden\` | 标准答案库 | **要**（越攒越值钱） |
| `schemas\` | 各类单据的识别规则 | 要（很小） |
| `aliases.json` | 成分词典 | 要（很小） |
| `FEEDBACK_LOG.md` | 错题本 | 要（很小） |
| `validate_export.py` | 核心脚本（唯一裁判） | 要（很小） |
| `AGENTS.md` | 行为手册（AI 的规矩） | 要（很小） |
| `使用说明.md` | 大白话操作手册 | — |
| `docs\` | 方案、评审、本文档 | — |
| `tools\` | 安装 / 打包 / 自检工具 | 要（很小） |
| `vendor\wheels\` | 离线依赖包（换电脑用） | 要（约 263 KB） |
| `.dsh\` `.agents\` | 项目级技能副本（安装脚本生成） | 不用（可重新生成） |
| `发布\` | 打包产物 | 不用 |

### 技能目录（以 Codex 为例）`.codex\skills\image-data-recognition\`

见本文第五节。

---

## 十、日常使用（装好之后）

1. 照片丢进 `inbox\`；
2. 对 AI 说：**"处理今天的图"**；
3. 审核它生成的 `task_review.xlsx`（只填"问题清单"那一列）；
4. 说一句"**审核完成**"；
5. 拿到 `final.xlsx`，照片已自动归档。

---

## 十一、常见问题

**Q：bat 双击一闪而过？**
右键"以管理员身份运行"，或先开"命令提示符"再运行，报错就能看见了。

**Q：提示找不到 Python？**
去 python.org 下载 3.10 以上版本；安装界面最下面**务必勾选 "Add python.exe to PATH"**，装完再双击安装脚本。

**Q：能不用 GitHub 吗？**
能，而且**本来就该不用**。整个系统就是一个文件夹，复制即迁移。GitHub 是给程序员传代码用的，你们不需要。

**Q：某个工具里没看到这个技能？**
1. 先确认那个工具的配置目录存在（比如 `%USERPROFILE%\.cursor\`）；
2. 重新跑一次 `tools\安装技能.py`；
3. 重启那个工具；
4. 还不行就告诉 AI："技能没被识别到，帮我查一下这个工具读哪个目录。"

**Q：怎么升级系统？**
拿到新版压缩包，解压后**只覆盖 `技能源文件\`、`validate_export.py`、`AGENTS.md`、`tools\`、`schemas\README`**，
**不要覆盖** `runs\`、`golden\`、`archive\`、`aliases.json`、`FEEDBACK_LOG.md`（这些是你的数据）。
覆盖完重跑一次 `tools\安装技能（双击）.bat`。

**Q：数据怎么做备份？**
复制 `archive\` + `runs\` + `golden\` 三个文件夹到移动硬盘或网盘即可。恢复时放回原位。

**Q：能不能放在共享盘多人用？**
可以放共享盘，但**同一时间只让一个人处理**（避免两个任务同时改同一个文件）。多人协作更适合各自电脑跑，最后汇总 `golden\`。

**Q：公司电脑不让装软件、也不让跑脚本怎么办？**
把 `技能源文件\image-data-recognition\` 文件夹手动复制到对应工具的技能目录（见第七节），再用记事本建一个 `workspace.txt` 写一行路径就行。
